# Overview

`poster2json` is a Python package that converts posters to JSON format for Posters.science.

## Installation

```bash
pip install poster2json
```

Or with Poetry:

```bash
poetry add poster2json
```

## Usage

The package provides example modules you can extend:

- **poster2json.generate** – example generation (e.g. `generate_example_json`)
